
class AppFonts {
  /// Scales the given font size down by a factor to make the UI less cluttered globally.
  static double size(double baseSize) {
    return baseSize * 0.85; // 15% reduction globally
  }
}
