--- lib/screens/reported_posts_screen.dart
+++ lib/screens/reported_posts_screen.dart
@@ -3,18 +3,33 @@

 class ReportedPostsScreen extends StatelessWidget {
   const ReportedPostsScreen({super.key});

+  @override
+  Widget build(BuildContext context) {
+    return const ReportedPostsScreenContent();
+  }
+}
+
+class ReportedPostsScreenContent extends StatefulWidget {
+  const ReportedPostsScreenContent({super.key});
+
+  @override
+  State<ReportedPostsScreenContent> createState() => _ReportedPostsScreenContentState();
+}
+
+class _ReportedPostsScreenContentState extends State<ReportedPostsScreenContent> {
+  late final Stream<QuerySnapshot> _reportedPostsStream;
+
+  @override
+  void initState() {
+    super.initState();
+    _reportedPostsStream = FirebaseFirestore.instance.collection('reported_posts').orderBy('reportedAt', descending: true).snapshots();
+  }
+
   @override
   Widget build(BuildContext context) {
     return Scaffold(
       appBar: AppBar(
         title: const Text('Şikayet Edilen Gönderiler'),
       ),
       body: StreamBuilder<QuerySnapshot>(
-        stream: FirebaseFirestore.instance.collection('reported_posts').orderBy('reportedAt', descending: true).snapshots(),
+        stream: _reportedPostsStream,
         builder: (context, snapshot) {
